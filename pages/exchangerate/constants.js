Urls = {
    'API_ENDPOINT' : 'https://api.exchangeratesapi.io'
}

Formats = {
     'DATE_FORMAT' : '%d-%d-%d',
     'DATE_FORMAT_REGEX' : '\\d{4}-\\d{2}-\\d{2}'
}

module.exports = {
    Urls : Urls,
    Formats : Formats
}